<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Rubik:ital,wght@0,300..900;1,300..900&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
      }

      html {
        font-family: "Rubik", sans-serif;
        font-weight: 400;
        font-size: 62.5%;
        color: #343a40;
      }

      body {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
      }

      .wrapper {
        display: flex;
        flex-direction: column;
        gap: 9.6rem;
        margin: 0 2.4rem;
        border-radius: 1rem;
        padding: 6.4rem 9.6rem;
        border: 0.2rem solid #495057;
      }

      .heading {
        font-size: 4.8rem;
        line-height: 1.1;
        letter-spacing: -0.1rem;
      }

      .buttons {
        display: flex;
        gap: 1.2rem;
      }

      .btn:link,
      .btn:visited {
        display: block;
        padding: 1.2rem 2.4rem;
        text-transform: uppercase;
        font-size: 1.8rem;
        font-weight: 600;
        text-decoration: none;
        cursor: pointer;
        color: #f8f9fa;
        border-radius: 1rem;
        background-color: #339af0;
        transition: all 0.5s;
      }

      .btn:hover,
      .btn:active {
        background-color: #1c7ed6;
      }

      .btn--admin:link,
      .btn--admin:visited {
        background-color: #ff6b6b;
      }

      .btn--admin:hover,
      .btn--admin:active {
        background-color: #f03e3e;
      }

      @media (max-width: 640px) {
        html {
          font-size: 55%;
        }

        .wrapper {
          text-align: center;
          align-items: center;
        }
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <h2 class="heading">Control your competitions</h2>
      <div class="buttons">
        <a class="btn btn--guest" href="guest/games">As guest</a>
        <a class="btn btn--admin" href="admin/games">As admin</a>
      </div>
    </div>
  </body>
</html>
